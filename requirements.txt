pywebview>=5.0        # janela nativa (usa o WebView2 do Windows)
google-genai>=2.0     # geracao de questoes com Gemini
pydantic>=2.7         # validacao da resposta da IA

# Só para empacotar/gerar arte — não precisa para rodar do código:
pyinstaller>=6.0
pillow>=10.0
